<article class="user-card <%= single ? 'single' : '' %>">
  <%= image_tag(user.photo) %>
  <div>
    <h1>
      <%= user.name %>
    </h1>
    <span>
      Number of posts: <%= user.posts_counter %>
    </span>
  </div>
</article>
